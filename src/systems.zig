const std = @import("std");
const SDL = @import("sdl2-zig");
const Image = SDL.image;
const Ttf = SDL.ttf;
const GameEngine = @import("engine.zig");

pub fn collidesRect(target: SDL.Rectangle, rest: []SDL.Rectangle) bool {
    
}
